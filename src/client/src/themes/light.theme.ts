import { createMuiTheme } from '@material-ui/core';
/**
 * only for example
 */

export const lightTheme = createMuiTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#4167B2',
    },
  },
});
